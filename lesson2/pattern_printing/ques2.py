# from video 

line = '*'
max_length = 10

while len(line) < max_length:
    print(line)
    line += "*"


# we can just change the variables according to our convenience doesn't matter output will me same 

star = '*'
max_length = 10

while len(star) < max_length:
    print(star)
    star += "*"